#include <iostream>
#include <algorithm>
using namespace std;
int s_swap (int a[],int ,int, bool(*p)(int))
{
    int sum=0;
    for(int i=0;i<end;++i)
    {
        sum=sum+a[i];
    }
    return sum;
}

int main()
{
    int (*f)()
    int a[]={5,1,2,3,4};

    cout << psum(c,b)<< endl;
    cout << psub(b,c)<< endl;
    return 0;
}
